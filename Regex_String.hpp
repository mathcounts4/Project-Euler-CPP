#pragma once

#include <regex>
#include <string>

/*
Work on sometime:
classes and functions designed to construct regular expressions
so that their meaning is clear,
but without creating a bunch of strings along the way.
(This uses lazy evaluation)
*/
